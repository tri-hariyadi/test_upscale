name: Deploy Client to Vercel

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

on:
  workflow_run:
    workflows: ["CI-Client"]
    types:
      - completed
    branches:
      - master

jobs:
  build-and-deploy:
    if: >
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Install Vercel CLI
        run: yarn global add vercel@latest

      - name: Pull Vercel Environment Information
        run: cd todo-app-client && vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Debug folder
        run: ls -lah && ls -lah todo-app-client

      - name: Build Project Artifacts
        run: cd todo-app-client && vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: cd todo-app-client && vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
